# هيل كافيه — دليل المشروع

دليل مختصر للإدارة: ما هو المشروع، كيف يعمل، ومن يستخدم كل شاشة.

---

## ١. المشروع في سطرين

منظومة كاملة لمقهى هيل في إربد سيتي سنتر، من لحظة ما يفتح الزبون المنيو حتى
تُقفل الفاتورة: **موقع تعريفي للزبائن** و**نظام تشغيل داخلي للطاقم**
يشتركان في نفس المنيو ونفس الهوية، وكل الشاشات **متزامنة لحظياً بين
الأجهزة** عبر قاعدة سحابية.

## ٢. الجزءان — وهذا أهم ما يجب فهمه

| الجزء | الرابط | لمن |
|---|---|---|
| **موقع الزبون** | `venusgamming2026g.github.io/Hail-Cafe/` | تعريف، منيو تصفح، حجز، ودخول الطلب |
| **نظام التشغيل HAIL OS** | نفس الموقع تحت `/hail-os/` | الطاقم: مطبخ، صالة، كاشير، إدارة |

**النظام واحد وحقيقي**: طلب من جوال الزبون على الطاولة يظهر على شاشة
المطبخ خلال ثانيتين، ولو كانا على شبكتين مختلفتين — التزامن عبر قاعدة
Supabase سحابية، بلا خادم خاص وبلا اشتراك مدفوع.

**النشر تلقائي بالكامل**: أي تعديل يُرفع إلى GitHub يظهر على الموقع خلال
دقيقة (GitHub Pages + مرآة Netlify، كلاهما يقرأ مجلد `docs/`).

## ٣. كيف تشغّله محلياً

انقر مرّتين على **`HAIL-OS.cmd`** — يفتح النظام على `http://localhost:3000`
وتظهر عناوين الشبكة لتجربته من الجوال. (بديل يدوي:
`node hail-os/server.mjs 3000`.)

الموقع الحيّ لا يحتاج تشغيلاً — هو منشور دائماً على الرابط أعلاه.

## ٤. دليل موقع الزبون

1. **الرئيسية** — الكفر، مراحل الطلب، اختيار رقم الطاولة، الأطباق
   المميزة، أجواؤنا، احجز، الفرع والخريطة.
2. **منيو التصفح** (`/menu.html`) — كل الأصناف بتصميم أسود×ذهبي يتبع ثيم
   جهاز الزبون، مولَّد آلياً من منيو النظام فلا يختلف عنه أبداً.
3. **منيو الطلب** (يُفتح من QR الطاولة أو من اختيار رقمها) — نفس التصميم
   الفاخر، مع سلة وخيارات لكل صنف، اسم لكل شخص لتقسيم الحساب، تتبّع حيّ
   لحالة الطلب، نداءات خدمة، والفاتورة.
4. **الحجز** يفتح محادثة واتساب برسالة جاهزة — التأكيد بالمحادثة.

## ٥. دليل شاشات النظام (الدخول بالأرقام السرية)

| الشاشة | من يستخدمها | الرقم |
|---|---|---|
| **المطبخ** `kds.html` | الشيف والباريستا — توجيه تلقائي للمحطات، مؤقّت وتصعيد للمتأخر | `4444` / `5555` |
| **الصالة** `floor.html` | النُّدُل — خريطة ٢٤ طاولة، النداءات بالأولوية، الجاهز للتقديم، الحجوزات | `3333` / `3434` |
| **الكاشير** `cashier.html` | التحصيل — الفواتير المفتوحة، تقسيم بالأسماء، خصم وبقشيش، طلب يدوي، الوردية | `2222` |
| **الإدارة** `admin.html` | المدير — مبيعات لحظية، مقارنة بأمس، المنيو والأسعار، المخزون، الطاقم، التقارير | `1111` |

المدير يدخل كل الشاشات، وإدارة الطاقم وأرقامهم من شاشة الإدارة.

## ٦. رحلة الطلب كاملة

1. الزبون يمسح QR طاولته (أو يختار رقمها من الموقع) ويكتب اسمه.
2. يختار أصنافه ويرسل — يظهر الطلب في **المطبخ** موزّعاً على المحطات.
3. الشيف: قبول ← تحضير ← جاهز — و**الصالة** تُنبَّه للتقديم.
4. الزبون يتابع الحالة على جواله، يطلب جولات إضافية أو خدمة.
5. «اطلب الفاتورة» تصل الكاشير — تحصيل كامل أو مقسّم بالأسماء.
6. تُقفل الجلسة، الطاولة «تحتاج تنظيف» ثم «فارغة»، وكل شيء في التقارير.

## ٧. تعديل المنيو (الأكثر تكراراً)

يُعدَّل ملف واحد: `hail-os/assets/js/data/menu.js` (الأصناف والأسعار
والأقسام)، ثم أمران: `npm run sync` ثم رفع إلى GitHub — فيتحدّث منيو
الطلب ومنيو التصفح معاً تلقائياً. تعديلات سريعة (سعر، إخفاء صنف نافد)
تتم مباشرة من شاشة الإدارة ← المنيو دون أي ملفات.

## ٨. ما يحتاج قرارك

| # | الموضوع | الوضع |
|---|---|---|
| ١ | **صور الأصناف** | ٣٥ من ٢٠٩ لها صور حقيقية؛ الباقي يستخدم صورة قسمه. يحتاج جلسة تصوير. |
| ٢ | **اسم صنف بيتزا** | صنف بسعر ٤٫٩٥٠ اسمه غير ظاهر في المنيو الأصلي — موسوم في النظام بانتظار الاسم. |
| ٣ | **الحجز** | عبر واتساب حالياً؛ نقله داخل النظام (شاشة حجوزات للإدارة موجودة) قرار تشغيلي. |
| ٤ | **رصيد Netlify** | المرآة تتوقف عند انتهاء رصيد الحساب المجاني وتعود تلقائياً؛ GitHub Pages لا يتأثر. |

## ٩. حالة الجودة عند التسليم

- ثمانية اختبارات آلية تنجح بلا أي تنصيب (`npm test`).
- الاختبارات تحرس: منطق الطلبات والفواتير، سلامة المزامنة، تطابق نسخة
  النشر مع المصدر، وعدم تسرّب أي مفتاح سري لملفات المتصفح.
- لا أخطاء في وحدة تحكم المتصفح، RTL كامل، أهداف لمس ≥ 44px، واحترام
  تقليل الحركة.
- التفاصيل التقنية كاملة في `README.md`.
